name: dbt-gold
entrypoint: prefect/flows/dbt_gold.py:dbt_gold
work_pool:
  name: default
parameters: {}
schedules:
  - cron: "15 * * * *"
    timezone: UTC
    active: true
concurrency_limit:
  limit: 1
  collision_strategy: ENQUEUE
tags:
  - dbt
  - gold
