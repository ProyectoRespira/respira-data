name: gold-then-inference
entrypoint: prefect/flows/gold_then_inference.py:gold_then_inference
work_pool:
  name: default
parameters: {}
schedules:
  - cron: "20 * * * *"
    timezone: UTC
    active: true
concurrency_limit:
  limit: 1
  collision_strategy: ENQUEUE
tags:
  - dbt
  - inference
  - coordinator
